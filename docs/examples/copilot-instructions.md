## Dataverse

Use the `dataverse-dev` MCP tools when live Dataverse information is useful.

Connections for this project:

- `YOUR-dev` - primary development environment
- `YOUR-test` - test environment
- `YOUR-prod` - production environment

Default to `YOUR-dev` unless another connection is explicitly requested.

The primary Dataverse solution is `YOUR_DEFAULT_SOLUTION`.

Treat production as read-only unless the user explicitly requests otherwise.

Do not perform destructive operations without explicit user confirmation.

Make no mistakes🫠!